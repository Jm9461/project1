package org.org.asm;

import android.os.IBinder;

class PdfSpotColor
  implements c
{
  private final IBinder altcs;
  
  PdfSpotColor(IBinder paramIBinder)
  {
    altcs = paramIBinder;
  }
  
  public boolean equals(Object paramObject)
  {
    return ((paramObject instanceof PdfSpotColor)) && (altcs.equals(altcs));
  }
  
  public int hashCode()
  {
    return altcs.hashCode();
  }
}
