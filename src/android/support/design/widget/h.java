package android.support.design.widget;

import java.lang.ref.WeakReference;

class h
{
  boolean b;
  final WeakReference<t.b> v;
  int w;
  
  boolean c(Number paramNumber)
  {
    return (paramNumber != null) && (v.get() == paramNumber);
  }
}
